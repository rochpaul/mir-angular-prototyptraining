import { ElementremovePipe } from './elementremove.pipe';

describe('ElementremovePipe', () => {
  it('create an instance', () => {
    const pipe = new ElementremovePipe();
    expect(pipe).toBeTruthy();
  });
});
